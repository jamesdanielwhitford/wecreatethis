# Session 011 - 2026-07-29

Implemented the reveal-frontier fix for the blog's layout-shift bug left open at
the end of session 010. CLS is now 0 on every route and Lighthouse Agentic
Browsing reaches 100.

## What was done

Followed the implementation checklist in `session-010-cls-diagnosis.md`.

- **Reverted the `content-visibility` attempt** that was sitting uncommitted in
  the working tree. It measured 0.456, worse than the 0.298 in prod.
- **`style.css`**: `.post-entry:not(.is-revealed) { visibility: hidden }`,
  replacing the `content-visibility` block.
- **`app.js`**: added `revealLoaded()`, which walks the stack and reveals the
  contiguous prefix of rendered posts. Called after render, after a failed
  fetch, and after the reading-order toggle reorders the stack.
- **`sw.js`**: `CACHE_NAME` bumped `blog-v11` → `blog-v12`, so users' stale
  cache-first shells are purged promptly.

## Two deviations from the written recommendation

Both found while implementing, neither changes the approach.

- **`revealLoaded` toggles `is-revealed` both ways.** The recommendation only
  ever *added* the class and `break`ed at the first unloaded post. After the
  reading-order toggle that is wrong: reversing the stack can put an already
  revealed post *below* an unloaded one, which then gets displaced when that
  post finally lands. Now it sets a flag and uses
  `classList.toggle('is-revealed', atFrontier)` across every entry, so posts
  hide again when the order moves them behind the frontier.
- **`revealLoaded` is defined above `loadEntry`, not after `entries`.** Function
  declarations hoist within the scope so either position works; above the first
  caller just reads better.

## Verification

Slow 4G, 412x823 mobile, isolated browser context, service worker defeated with
the `navigator.serviceWorker` override, against `wrangler pages dev` on :8080.
Confirmed the server was actually serving the new code (grepped `app.js` for
`revealLoaded`) before trusting any number.

| Route | CLS |
|---|---|
| `/blog/test` cold load | **0.000**, all 4 posts loaded and revealed |
| Deep link `#ordering-and-dates` | **0.000**, target at 24px (matches `scroll-margin-top`) |
| Deep link to heading anchor `#h-tables` | **0.000**, heading resolves, lands at 24px |
| Reading-order toggle after load | **0.000** residual, order reversed, nothing stuck hidden |
| `/blog/dev-tools` (single post, dark mode) | **0.000** |
| `/blog/test/nested` (nested section) | **0.000**, subsection link intact |

**Failed-fetch case**: forced `kitchen-sink/index.md` (the *first* post in the
stack, the worst case) to reject. It shows "Failed to load post.", the frontier
moves past it, and all three posts below it still reveal. Nothing stuck hidden.

**Lighthouse** on local `/blog/test`, mobile: **100 / 100 / 100 / 100**, 52
passed, 0 failed. `cumulative-layout-shift` audit: score 1, value 0. Agentic
Browsing was 80 in prod before this.

## Shipped to prod

Commit `d37b583` on `dev`, merged fast-forward to `main` as `15d2c38` and
pushed. Cloudflare picked it up in ~40s. The merge also carried session notes
008-011, which had never reached `main`.

Verified against live `https://wecreatethis.com/blog/test`, Slow 4G, mobile:

- **CLS 0.000** with the SW defeated, new code confirmed running.
- **Lighthouse 100 / 100 / 100 / 100**, 52 passed, 0 failed, no failing audits
  at all. `cumulative-layout-shift`: score 1, value 0. **Agentic Browsing went
  80 → 100**, which was the whole point.
- **With the real service worker active**: CLS 0.000, cache is `blog-v12`.

One thing that looks like a bug and is not: with the SW active, only
kitchen-sink loads on first paint and the other three sit `visibility: hidden`
with `data-loaded="false"`. They were never *fetched* - kitchen-sink renders to
3949px and pushes them past top 4300px, well outside the observer's 600px band.
Scrolling down loads and reveals all of them, nothing stuck hidden. Scrolling
the full page end to end produced 0.0086, far under the 0.1 threshold and not
part of the load-time score.

## Not done

- **`content/test/` is still published, not draft**, and still linked from the
  blog homepage. Unchanged from session 010; flipping it to `draft: true` is
  the intended cleanup once testing is done. It is currently the main fixture
  for this bug, so it was deliberately left live.
- The **SEO ceiling** (posts as `#fragment`s on a shared section URL) and
  build-time image optimisation remain deferred, as in session 010.
