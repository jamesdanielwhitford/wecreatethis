# Sessions Overview

## Session log

| # | Date | Key outcomes |
|---|------|-------------|
| 001 | 2026-05-30 | Voice Notes rebuilt: worker + D1 backend, admin login, live 10s chunked transcription, editable notes, wake lock |
| 002 | 2026-05-30 | Voice Notes: fix notes never loading (IndexedDB index key mismatch), remove live streaming, add mic-drop detection, push to prod |
| 003 | 2026-07-09 | Hardle: merged Hardle+Randle into one page (Practice Mode, no separate URL), removed Easy Mode, dot-based scoring, Zanagrams-style results screen + hamburger menu with in-modal feedback view. Branch `hardle-update` pushed, not merged. |
| 004 | 2026-07-09 | Hardle: merged `hardle-update` to `main` (live in prod). Fixed tile squashing on narrow screens, dead gap on tall screens (iPad Pro), daily puzzle resetting after switching to Practice Mode, and Enter/Backspace key shading. |
| 005 | 2026-07-14 | Blog: full rebuild to folder-driven content (`content/{section}/{post}/index.md` + `build.js` manifest generator), scroll-through post stack with `#fragment` URLs, hand-authored `content/home.md` homepage, `draft: true` frontmatter flag. Merged `blog-work` → `dev` → `main`, live in prod. |
| 006 | 2026-07-17 | Blog: nested sections (any folder depth), unified breadcrumb layout + shared `style.css`, zero-maintenance `_redirects` (content pass-through + wildcard), CI manifest regeneration (GitHub Action), reading-order toggle, code fences/inline code/blockquotes with a tiny no-dependency highlighter, SW v7 (manifest-derived assets, network-first content, cache-poisoning fix). Uncommitted on `dev`. |
| 007 | 2026-07-17 | Site-wide caching rework: all 15 non-blog SWs converted to one canonical network-first strategy (3s timeout, cache fallback, shell fallback); version-bump ritual retired; internal `.html` links fixed (git-notes, longform, pokemon, starrynight); homepage validScopes bug fixed (was unregistering blog/chesstimer/chessle/inkwell/symbolicritual SWs). Executed via Haiku subagents, verified with Playwright. On `dev`, not yet in prod. |
| 008 | 2026-07-17 | New app **Bird Bingo**: standalone from Birdle, own IndexedDB, daily 3x3 + practice (3x3/4x4/5x5) bingo cards built from real nearby eBird sightings (tight-radius, frequency-ranked), own life list, bird detail page with Wikipedia description/iNaturalist call/sighting log, hard mode, CSS celebrations. Fixed Wikipedia disambiguation pages blocking image fallback, and daily card regenerating on GPS jitter instead of real movement. Live in prod on `main`. |
| 009 | 2026-07-24 | Bird Bingo redesign in two waves: (1) grid-first dark layout modeled on the mobile game Zanagrams, settings moved into a shared header menu, practice mode changed from unlimited concurrent cards to one regenerating card, bingo win shows a celebration modal (Share Score, matching Hardle's share pattern) overlaying the grid plus a secret "full house" tier; (2) light/dark theme toggle (defaults to system preference) via CSS custom properties, every emoji replaced with inlined Lucide icons, accent color sampled from the real app icon, header Home button uses the app icon image and links out to wecreatethis.com from the app's own homepage. Live in prod on `main`. |
| 010 | 2026-07-29 | Blog: fixed renderer bugs breaking the live post (bold-led paragraphs unwrapped, no tables/ordered lists, dead TOC anchors, duplicate titles, `//` in URLs highlighted as comments); system dark mode via `prefers-color-scheme` (blog only, no toggle); contrast raised to WCAG AA; added `robots.txt` + `llms.txt` (both were absent, so Cloudflare served HTML to crawlers) and a generated `blog/sitemap.xml`; `build.js` now reads image dimensions from file headers and validates frontmatter; SW shell moved to stale-while-revalidate with the shared update toast (`blog-v11`). Lighthouse 100/100/100/100 locally. **Open: CLS ~0.298 on live `/blog/test`, four failed fix attempts, Fable subagent diagnosis pending.** |
| 011 | 2026-07-29 | Blog: implemented the "reveal frontier" fix for the CLS bug left open in 010. Posts are revealed only once they and every post above them have rendered (`revealLoaded()` + `.post-entry:not(.is-revealed) { visibility: hidden }`), so late-arriving content is only ever appended below what is painted, never inserted above it. Reverted the `content-visibility` attempt (measured 0.456, worse than the bug). SW `blog-v12`. **Live in prod (`15d2c38`): CLS 0.000 and Lighthouse 100/100/100/100 on `wecreatethis.com/blog/test`, Agentic Browsing 80 → 100.** Verified on section load, post deep link, heading anchor, sort toggle, dark mode, nested section, and a forced-failure test. |
| 012 | 2026-07-29 | Bird Bingo research (no app code changed): reverse-engineered Merlin's "Likely Birds" ordering. **It is eBird bar-chart frequency for the region, sliced to the current week** - confirmed 21/24 recall, Kendall tau +0.667 vs City of Tshwane (`ZA-GT-08`). Data is behind Cornell SSO (`ebird.org/barchartData`), not the public API. **Also found a real bug: `/data/obs/geo/recent` returns one de-duplicated row per species (183 records/183 species/max count 1), so `bingo.js`'s occurrence counter always lands on 1 - the `"N checklists"` metric is fictional and card ordering is arbitrary, regardless of radius.** Best globally-available method is hotspot-nearest frequency (9/9 top-9, tau +0.50, ~212 calls) but eBird 429s after ~1-2k calls, so no ~200-call method can run live per card. All artifacts were in `birdle-tests/merlin-reverse-engineering/`, sitting untracked on `dev`/`main` indefinitely; **session 022 moved them to their own branch, `archive/birdle-tests-merlin-research`** (pushed to origin), so check out that branch to see the actual files - they're no longer in the working tree of `dev`/`main`. |

| 013 | 2026-07-29 | **CV/portfolio pages published to prod (4 PRs, no app code).** Started as a search for session notes on recent CV/application work - there were none, in either location. Found ~189/245 lines of **uncommitted** edits across `cv/`, `cv-fde/`, `portfolio-fde/`, `technical-writing-portfolio/` (unstaged since 18 May), plus an unlogged journalism application in `JobApplications/broad-media/` (Broad Media, written 28 Jul, no `CLAUDE.md`/`sessions/`, not a git repo). Published the pending edits (tenure 3 → 4 years; HN claim broadened from one article + "1.5k+ visits" to four named pieces; new "Hacker News Front Page" section atop both portfolios; six Letta patterns un-drafted; three new TechStackups pieces). Added the Mr D agentic-ordering piece and the SA payment-processor comparison to both CVs and Mr D to both portfolios, pruning weaker cards (`cv/` → 5, `cv-fde/` → 6; VirtualFit kept by request, Zig dropped from `cv/` only). Regenerated all four PDFs, which were badly stale. Fixed a print bug where `cv-fde` page 1 was mostly blank. |
| 014 | 2026-08-04 | Minor housekeeping, no app code changed. Deleted two stale Syncthing-style duplicate files found untracked in the repo (`cv/index 2.html`, `portfolio-fde/index 2.html`) - both were older snapshots (3-years tenure, missing recent articles/projects) superseded by the current `index.html` in each folder, confirmed via diff before removing. |
| 015 | 2026-08-04 | **Blog is now the homepage** (branch `blog-homepage` off `dev`, not yet merged). Physically moved every blog file from `blog/` to the repo root and rewrote every `/blog/` path reference to `/` across `app.js`, both HTML shells, `sw.js`, `build.js`, `_redirects`, `robots.txt`, `llms.txt`, the GitHub Action, and the content markdown itself. Old app-hub grid (root `index.html`/`sw.js`/`manifest.json`/`styles.css`/icons) archived, not deleted, at `archive/app-hub/`. SW cache bumped to `wecreatethis-v26`. Verified locally with `wrangler pages dev` (homepage, sections, nested sections, content fetch, other apps' own routing untouched, contact page, archived page all resolve) and opened in the user's real Chrome. `blog/CLAUDE.md` renamed to `CLAUDE-blog.md` at root. Not committed to `dev`/`main` yet - staged on the branch. |
| 016 | 2026-08-04 | **Removed the blog's fragment-based scroll-stack** (same branch `blog-homepage`, commit `3653a37`, still not merged). Every folder and every post is now its own real route (`/{section...}/{post-slug}`) instead of `/{section}#{post-slug}`; a post page renders exactly one post standalone, with no lazy loading, no reveal-frontier, and no reading-order toggle (all removed, since nothing else is ever in the DOM to sequence against). `parseBlogPath()` in `app.js` now resolves section vs. post from the manifest against the full path. Sitemap gained a URL per post (15 total, up from 6), closing most of the SEO-ceiling gap. Rewrote `content/test/`'s prose (user's choice) to describe/exercise the new routing instead of the removed stack/toggle behavior. `CLAUDE-blog.md` updated to match. Verified locally: the exact requested URL, section TOC links, a single-post section, 3- and 4-level-deep nested posts, and graceful degradation of old `#fragment` URLs. |
| 017 | 2026-08-04 | **Redesigned the blog's top-level navigation** (branch `blog-homepage`, commits `c38ab9f` + `6af9467`, still not merged). New 3-slot header (`renderHomeHeader`/`renderSectionHeader`/`renderPostHeader`): homepage gets a reserved left spacer + centered wordmark + profile-avatar button opening a bio modal; section/folder pages get a back arrow + folder title + same profile button; post pages get a back button + TOC modal (post title as a scroll-to-top link, plus every H2 as a jump link) + fixed bottom prev/next bar, both fading out on scroll-down and reappearing on scroll-up or at the bottom (`setupPostScrollFade`). Post pages also reveal the title in the header's center slot once the real H1 scrolls out of view (`setupPostTitleReveal`, IntersectionObserver). Found and fixed a real bug: the new `icons.js` was missing from `_redirects`'s pass-through list, so it 404'd into the section-page shell HTML and broke every page with a ReferenceError; SW bumped to `wecreatethis-v27`. Verified locally via `wrangler pages dev` + chrome-devtools MCP: both header shapes at multiple nesting depths, TOC/bio modals, scroll-fade, light mode, 390px mobile width. |
| 018 | 2026-08-04 | Two bug reports on session 017's new post-page nav. **Fixed**: a trackpad fling past the top of a post page could hide the nav instead of keeping it visible, because `window.scrollY` genuinely goes negative during rubber-band overscroll and `setupPostScrollFade`'s `y > lastY` check had no floor - clamped `y` to `Math.max(0, window.scrollY)` and added a 24px "always visible near top" zone, same pattern as the existing bottom-of-page handling. User confirmed fixed in incognito (their first live retest was on a normal browser tab, which was still serving a stale service-worker-cached `app.js` from before the fix - not a real regression). **Not fixed**: a "dot"/text-sliver ghosting bug where the serif foot of a post title's first letter (e.g. the "A" in "A skill structure that improves itself") is faintly visible near the header during scroll. Two theories tried and both wrong per user confirmation (sticky-position compositor flash, then the header's `opacity`+`transform` fade transitioning while still overlapping the title) - both CSS attempts reverted per explicit instruction to stop chasing it. Root cause still unknown. |
| 019 | 2026-08-06 | Bio + TOC modals redesigned to fill the whole viewport instead of looking like floating dialog cards - same page shell (max-width/padding/background) as every real page, close button lands in the exact nav slot the opening button was in (`style.css` `.modal-overlay`/`.modal-card`/`.modal-close`; `app.js` both modal builders now wrap content in the shared `.top-nav` grid, left slot left empty/reserved per user's choice). Verified at 1280px and 390px in dark mode via chrome-devtools MCP. Along the way, found and fixed a stale `content-manifest.json` still listing a renamed-away `dev-tools` section (real folder is `agentic-engineering`) - not a wrangler or app bug, just local drift; `node build.js` regenerates it and normally CI does this on push. Also settled on `npx wrangler pages dev . --port 8080` as the correct way to run the app locally (matches prod `_redirects` routing; plain `python3 -m http.server` 404s on clean blog URLs). |
| 020 | 2026-08-07 | **Fixed the loose-list/indentation renderer bug open since session 014.** Root cause (an Opus subagent's analysis): every block-level regex in `renderMarkdown` required its marker at strict column zero, so any leading whitespace (the trigger case: a homepage link list item with one leading space) fell through to plain-paragraph rendering. Fixed across all block types (lists/headings/quotes/fences/tables tolerate up to 3 leading spaces, matching CommonMark), plus `*`/`+` bullets, indented sub-items no longer leak as raw unwrapped text, CRLF normalized. Verified via a standalone regex harness and a full old-vs-new regression diff across all real content (byte-identical elsewhere). Added `content/test/renderer-edge-cases/` as a permanent regression post. Homepage markdown now renders through the same `.md-content` styling as every other page (removed `#home-content`-scoped dimming/bullet-stripping CSS), per explicit user request. Retuned the whole color theme through several rounds of live user feedback: light-mode bg landed on `#bcbcbe` (from white), dark-mode bg moved to pure `#000` with text softened to `#cccccc` (full white read as too stark) - every dependent token re-derived from actual WCAG contrast math at each step, not scaled proportionally (a naive `--muted` scale-down briefly broke at 2.04:1 before being caught). Also fixed light-mode syntax-highlighting colors reading as too desaturated. Bio modal gained a GitHub link (`.bio-email` generalized to `.bio-link`/`.bio-links`, new inlined-Lucide `github` icon). Branch pushed to `origin/blog-homepage` for the first time (previously local-only across sessions 015-019). |
| 021 | 2026-08-08 | New topic stub posts (`kindlewriter`, `agent-hacking`, `agent-programmatics`, `lotus`, `heat`), each its own top-level folder after extensive back-and-forth clarifying "folder" vs. "section" terminology - no code changes needed, `build.js`/`app.js` routing was already correct. Fixed `misc/agent-screenshots` (moved from an orphaned `AgentWeirdness/`, added missing frontmatter, fixed broken relative image paths that were 404ing to the section shell for the post's whole life, removed a byte-identical duplicate image, renamed all images descriptively with real alt text). Added a `link:` fenced-block syntax to `renderMarkdown` so a homepage folder's title+description render as one clickable/hoverable `.link-card`. External links (`http(s)://`) now open in a new tab (`target="_blank" rel="noopener"`), internal links unaffected. Fixed a scope-creep bug where an underline fix meant only for `.link-card` had briefly also changed all post-body link underlining. **Bio modal fully redesigned**: real photo (`profile.jpeg`, compressed via TinyPNG, full width, `object-fit`/`max-height` letterboxing bug found and fixed), owner's name moved to the modal's own page-top wordmark slot, placeholder bio sentence removed, new live Pretoria/GMT+2 clock (`Intl.DateTimeFormat` against `Africa/Johannesburg`, re-renders every 30s) with a new reusable `Globes` emoji-icon system, new CV link (`/cv`). Nav profile button changed from text initials to a bust icon. **Site's first-ever favicon added** (`favicon.svg`, a "We" wordmark in the site's dark-mode colors/font). Three sections pulled off `content/home.md` per user request and saved to a new `DRAFT-TOPICS.md` for later writing; homepage is now just "Blog coming soon." `sw.js` `CACHE_NAME` bumped `v27` → `v28`. Branch `blog-homepage`, two commits this session, **merge to `dev`/`main` + prod deploy requested and approved but not yet executed** - see next session checklist. |
| 022 | 2026-08-08 | **Fixed a real iOS Safari bug**: post-page navbar had a see-through gap above it during overscroll (sticky `header` inside a padded `body` never actually reached the true top of the page). Fixed via a negative-margin/padding trick on `header` in `style.css`, iterated live against the user's actual phone over LAN (`wrangler pages dev --ip 0.0.0.0`) since Chrome desktop emulation can't reproduce iOS rubber-band overscroll; landed on a symmetric 24px gap above/below the navbar per user's explicit ask, verified via chrome-devtools MCP `evaluate_script`. **Added social-preview/iOS-icon support that never existed**: researched via `/better-research` (OGP spec, MDN, web.dev), then added `apple-touch-icon.png` (180x180, the actual missing piece - Safari never uses the SVG favicon for home-screen icons), `og:*`/`twitter:*` meta tags site-wide with a new `og-image.png` (1200x630 "We" wordmark) so shared links show a real preview on iMessage/Slack/Discord/Twitter, and regenerated `icon-192.png`/`icon-512.png` (PWA install icons) to match the wordmark instead of the old pencil-glyph app-hub icons. All description text trimmed to "Blog." site-wide (meta description, OG, Twitter, `manifest.json`). Known limitation flagged in code: previews are site-wide only for now (scrapers don't run JS, so per-post previews need `build.js` to emit static HTML - same gap as the existing SEO note). Live in prod (`7a2ebd5`, `dev` → `main` fast-forward). **Also archived long-untracked `birdle-tests/`** (session 012's Merlin research + a stray session-008 file) onto a new branch `archive/birdle-tests-merlin-research`, pushed to origin, removed from `dev`/`main`'s working tree entirely. |

## Current status

- **Blog is now the homepage, and every post has its own route (sessions 015-021, merged `blog-homepage` → `dev` → `main` and live in prod as of session 021, 2026-08-08 - verified directly on `wecreatethis.com`).** Everything moved from `blog/` to the repo root: `index.html`, `section.html`, `app.js`, `style.css`, `sw.js` (still `wecreatethis-v27`, from session 017 - the modal/theme/renderer JS+CSS changes from sessions 019-020 are committed but the cache version has not been bumped for them, so users may need a hard refresh until it is), `manifest.json`, `content-manifest.json`, `sitemap.xml`, `build.js`, `content/`. Every `/blog/` path reference was rewritten to `/`. The old app-hub grid (the icon grid that used to be the homepage) is **archived, not deleted**, at `archive/app-hub/` - it still loads if visited directly but its internal links are stale, which is fine since nothing links to it anymore. `blog/CLAUDE.md` is now `CLAUDE-blog.md` at the repo root (still gitignored). **Session 016 removed the old fragment-based scroll-stack entirely**: posts used to render as one continuous scroll-through stack per section, addressed by `/{section}#{post-slug}`, with lazy loading and a "reveal frontier" to keep CLS at zero. Now `/{section...}/{post-slug}` is a real route and a post page renders exactly one post, standalone - no stack, no lazy loading, no reveal-frontier, no reading-order toggle (all removed). `parseBlogPath()` in `app.js` resolves section vs. post from the manifest against the full URL path. The sitemap now lists a URL per post, not just per section. `content/test/` was rewritten to test the new routing model instead of the old one. **Session 017 redesigned the top nav**: 3-slot header (spacer/back-arrow, title, profile-avatar-with-bio-modal) shared across homepage/section/post pages, post pages additionally get a TOC modal and a fixed prev/next bottom bar that both fade on scroll. **Session 018 fixed a real bug in the scroll-fade** (top-of-page trackpad-bounce could wrongly hide the nav, since `window.scrollY` goes negative during rubber-band overscroll - now clamped + given a top dead-zone) but **left an unfixed, unreverted-theory "dot" ghosting bug open**: a sliver of a post title's first letter is visible near the header during scroll, cause still unknown after two wrong theories, both attempted CSS fixes reverted per user instruction to stop chasing it. **Session 019 redesigned the bio and TOC modals to be full-viewport pages instead of floating dialog cards** - same page shell as every real page, close button lands in the exact nav slot the opening button was in, left slot reserved/empty by design. **Session 020 fixed the long-open loose-list/indentation rendering bug** (every block-level regex in `renderMarkdown` required column-zero anchoring; now tolerates up to 3 leading spaces on lists/headings/quotes/fences/tables, `*`/`+` bullets work, indented sub-items no longer leak as raw text, CRLF normalized - see `content/test/renderer-edge-cases/` for the full regression set), **made the homepage render through the same `.md-content` styling as every other page** (no more homepage-specific dimming/bullet-stripping), **retuned the full color theme** (light bg → `#bcbcbe`, dark bg → pure `#000` with softened `#cccccc` text, every dependent token re-verified against WCAG contrast math), and **added a GitHub link to the bio modal**. **Session 021 finished the bio modal** (see its row above for full detail): real photo, name as page-top wordmark, live location clock, CV link, first-ever favicon, `sw.js` bumped to `v28`. **That session's work, plus everything from 015-020, is now merged and live in prod on `main`.** **Session 022 fixed a real iOS Safari post-page navbar overscroll bug** (negative-margin/padding trick on `header`, verified live against the user's phone over LAN) **and added social-preview/iOS-icon support that never existed** - `apple-touch-icon.png`, `og:*`/`twitter:*` meta tags, `og-image.png`, and a wordmark-matched `icon-192.png`/`icon-512.png` - live in prod as of `7a2ebd5`. Per-post OG previews still need `build.js` to emit static per-post HTML (site-wide-only for now, by explicit user choice); see that session's row for the full iteration story and the `mask-icon`/`viewport-fit=cover` attempts that were tried and reverted.
- **CV and portfolio pages are current and live in prod** (session 013, PRs #19-#22). Both CVs and both portfolios now say 4 years, claim "multiple pieces" on the Hacker News front page (Cerebrium/vLLM, Sourcegraph Zig, tldraw, GLM) rather than one article with a 1.5k+ visits figure, and lead the portfolios with a "Hacker News Front Page" section. Featured cards: `cv/` has 5 (Cerebrium/vLLM, Sentry MCP, Express+HTMx, Mr D, SA payment processors), `cv-fde/` has 6 (Letta patterns, Cerebrium/vLLM, Sentry MCP, VirtualFit, Mr D, SA payment processors). **All four PDFs were regenerated** and now match their pages. **PDFs are generated, not hand-authored**: serve the repo, render with headless Chrome `--print-to-pdf`, and inject `.no-print{display:none !important}` into a temp copy first - the existing rule sits inside `@media print`, which `--print-to-pdf` does not reliably apply, so the Print/Download buttons otherwise render into the file. **Verify PDFs by reading the rendered pages, not by text extraction** - the text is font-encoded and extraction reports every string as absent, including ones that are present. Print-CSS trap: `.job-entry` on `cv-fde` had `break-inside: avoid` while the `Experience` heading has `break-after: avoid`, so its nine-bullet first entry could not fit on page 1 and Chrome pushed heading and entry wholesale to page 2, leaving page 1 mostly blank; job entries may now split. **`cv/` still has the original rule and will hit the same bug if its first entry grows past eight bullets.** `cv-fde` is 3 pages with a short page 3 (two cards) - kept deliberately.
- **The journalism application in `JobApplications/broad-media/` is unlogged and unstructured.** Three files written 28 Jul for Broad Media: `cv-simple.html` (a de-teched "Writer & Software Developer" CV leading on sub-editing and fact-checking) plus tech and gaming cover letters. The gaming letter cites Hardle, Bird Bingo and the Pokémon calculator but the CV only links the *technical writing* portfolio, so there is no games portfolio behind it. Both letters say "My CV and portfolio are attached" - **nothing records whether they were sent, or to which listings.** That folder has no `CLAUDE.md` and no `sessions/`, and sits beside a loose `test.md` and a stray screenshot at the top of `JobApplications/`.
- Voice Notes live in prod (SW v13, DB voice-notes-v3)
- Worker deployed at `https://voice-notes.james-052.workers.dev`, D1 has 2 remote notes
- Single post-recording transcription (no more live streaming)
- Mic-drop stops animation immediately
- Not yet tested: admin login + D1 sync on mobile
- Hardle: Hardle/Randle merge is live in prod on `main` (commit `b896181`), including follow-up fixes for tile sizing, layout centering, and a daily-puzzle-reset bug. See session-004 for full detail.
- Blog: session-006 upgrade (nested sections, unified layout, CI manifest, sort toggle, code rendering, SW v7) is **live in prod** (merged to `main` in `459600c`). Architecture documented in `blog/CLAUDE.md`; the blog SW deliberately deviates from repo-wide SW rules.
- **Blog (session 010) is live in prod** (`b8d01b0`, SW `blog-v11`). The published post rendered wrong for its whole life and is now fixed: bold-led paragraphs were never wrapped in `<p>` (22 of them, merging into run-on text), tables and ordered lists were unsupported despite being used, its table of contents linked to heading ids that did not exist, the title rendered twice, and `//` inside URLs was greyed out as a comment. Blog now has system dark mode (`prefers-color-scheme`, no toggle, blog only) with WCAG AA contrast in both themes. `robots.txt` and `llms.txt` were both **absent**, so Cloudflare's fallback served HTML to crawlers - both added, plus a `build.js`-generated `blog/sitemap.xml`. `build.js` also reads image dimensions from file headers (hand-rolled PNG/GIF/JPEG parsing, no deps) into the manifest so the renderer can emit `width`/`height`, and warns on bad frontmatter. SW shell is now stale-while-revalidate with the shared `/sw-toast.js` pill; it was cache-first with no revalidation, which is why Safari showed new dark mode with the old renderer after a deploy.
- **Blog CLS bug FIXED and LIVE IN PROD** (session 011, `15d2c38`, SW `blog-v12`). The reveal frontier from `session-010-cls-diagnosis.md` is implemented: `revealLoaded()` in `app.js` reveals a post only once it and every post above it have rendered, with `.post-entry:not(.is-revealed) { visibility: hidden }` (not `display: none`, which would break the IntersectionObserver). **Live `/blog/test` now measures CLS 0.000 and Lighthouse 100/100/100/100 - Agentic Browsing went 80 → 100**, verified both with the SW defeated and with it active. Also verified locally on post deep link, heading anchor, sort toggle, a single-post section in dark mode, and a nested section, plus a forced-failure test confirming a failed fetch does not stall the frontier. Root cause was that the stack paints in manifest order but posts render in network-completion order, so the long first post landing late shoved already-painted siblings out of the viewport. **Height reservation cannot fix this and should never be retried** - estimates are viewport-width-dependent, and `content-visibility` measured 0.456, worse than the bug. Reasoning preserved in `session-010-cls-diagnosis.md`; architecture documented in `blog/CLAUDE.md`. Note: with the SW active only the first post loads on first paint (the rest fall outside the observer's 600px band once kitchen-sink renders to ~3949px) - they load and reveal normally on scroll, this is lazy loading working, not a stall.
- **Blog test content is live and publicly linked** from the blog homepage (`content/test/`, five posts including nested sections three deep). This was deliberate, to test on the real site. Flipping them to `draft: true` is the intended cleanup.
- Blog measurement gotcha: `blog/sw.js` served a stale `app.js`/`style.css` repeatedly during session 010 and silently invalidated several measurements. When testing, defeat the SW with an initScript `Object.defineProperty(navigator, 'serviceWorker', { get: () => undefined, configurable: true })` and confirm the code under test is actually running.
- Site-wide caching rework (session 007) is **live in prod** (`459600c`): every non-blog SW uses stale-while-revalidate + shared `/sw-toast.js` update pill (instant loads, same-visit update prompts); `cleanResponse()` redirect stripping on every cache.put; version bumps no longer needed for routine deploys. Verify on a real device: old cache-first SWs self-replace on next online visit. `dev/` folder is obsolete and pending deletion (permission-blocked). See session-007 for full detail.
- **Bird Bingo** (sessions 008-009) is **live in prod** (`0f28fa0`): standalone app at `/bird-bingo/`, own IndexedDB (`bird-bingo-db`). Session 009 redesigned the whole UI in two waves: (1) a Zanagrams-style dark, grid-first layout with settings behind a shared header menu (`menu.js`), practice mode as a single regenerating card instead of a list, bingo wins as a celebration modal with Share Score + a secret full-house tier overlaying the grid; (2) a light/dark theme toggle defaulting to system preference (`AppMenu.getTheme()`, CSS custom properties in `styles.css`), every emoji replaced with inlined Lucide SVG icons (`bird-bingo/icons.js`), accent color sampled from the real app icon (`#2d7a3a`), header Home button now the actual app icon image, linking to `wecreatethis.com` from the app's own homepage. Wave 1 was fully live-verified (no console errors, correct DB state after regenerate/toggle actions); wave 2 was partially verified (homepage + Settings modal confirmed working) before the browser session was interrupted - daily/practice/life/bird pages' post-wave-2 rendering and the Light toggle's actual re-theme effect still need a check. Not yet verified on a real device: GPS capture (a `kCLErrorLocationUnknown` warning was seen in Chrome dev console after this session - a macOS Core Location hiccup, not an app bug, but real-device testing is still open), native share sheet, mobile layout beyond one 390px check. Calculation method, data model, layout pattern, and celebration logic documented in `bird-bingo/CLAUDE.md`.
- **Bird Bingo card ranking is known-broken (session 012, research only, nothing changed in the app).** `bingo.js` `fetchCandidates()` ranks by counting occurrences per species from `/data/obs/geo/recent`, but that endpoint **de-duplicates to one row per species** (measured: 183 records, 183 distinct species, max count 1). Every species ties at 1, so the `"N checklists"` metric string is fictional and card order is eBird's arbitrary return order. Radius tuning changes which birds appear, never their ranking - which is why past tuning never helped. Merlin's list was reverse-engineered to **eBird bar-chart frequency for the region, sliced to the current week** (21/24, tau +0.667 for `ZA-GT-08`), but that lives behind Cornell SSO with no CORS at ~818KB/region, so it is only usable as pre-captured static data. Best global-and-instant alternative is hotspot-nearest per-checklist frequency (9/9 top-9, tau +0.50), but at ~212 calls per location and eBird 429ing after ~1-2k calls **no such method can run live per card** - caching per location is mandatory. James rated hotspot-nearest and distance-weighted best by reading the bird lists, ahead of what the metrics showed. Full detail, per-method API costs, and call-reduction options in `FINDINGS.md`/`METHODS.md` (plus `compare.html`/`budget.html` test pages) on branch `archive/birdle-tests-merlin-research` - moved off `dev`/`main`'s working tree in session 022, no longer present locally unless that branch is checked out. Still n=1 and unvalidated on a second region.

## Next session checklist

- [x] ~~Blog nav/theme/content iteration, merge to dev/main~~ - **done in session 021**: `blog-homepage` merged to `dev` then `main` (three clean fast-forwards), live in prod, verified directly on `wecreatethis.com` (homepage, bio modal photo/clock/links, favicon, `/cv`).
- [x] ~~Bump `sw.js`'s `CACHE_NAME`~~ - **done in session 021**, `v27` → `v28`.
- [x] ~~Fix iOS Safari post-page navbar overscroll gap~~ - **done in session 022**, live in prod.
- [x] ~~Add social-preview meta tags + apple-touch-icon~~ - **done in session 022**: OG/Twitter Card tags, `apple-touch-icon.png`, `og-image.png`, wordmark-matched `icon-192.png`/`icon-512.png`, all live in prod. Site-wide-only previews for now - per-post OG previews are the remaining piece (see below).
- [ ] **Fix the navbar "dot" ghosting bug** - a faint sliver of a post title's first letter (e.g. the serif foot of the "A" in "A skill structure that improves itself") shows near the header during scroll on post pages. Two theories tried and ruled out by the user in session 018 (sticky-position compositor flash; header's `opacity`+`transform` fade transitioning while still overlapping the title) - both CSS fixes reverted, root cause unknown. Needs a better repro method than static screenshots (a screen recording during the actual transition, or live debugging with the user) rather than more guessing from computed styles.
- [ ] **Fix the post-page back button using section-relative history** - flagged by the user in session 021: it always links to `/{section}` (hardcoded href), so following an internal cross-post link and clicking Back returns to the section TOC instead of the referring post. Needs real browser history (`history.back()`) or an explicit referrer-aware back target.
- [ ] **User wants to experiment with fonts** (session 021) - currently `Georgia, serif` site-wide, also baked into `favicon.svg`'s "We" text, so a font change should regenerate the favicon too if it's meant to stay visually consistent.
- [ ] Blog local dev: after renaming/adding a `content/` folder, run `node build.js` before testing so `content-manifest.json`/`sitemap.xml` don't drift stale (bit session 019 - a stale `dev-tools` → `agentic-engineering` rename wasn't reflected locally, briefly looked like a real rendering bug)
- [ ] Blog: consider a redesign pass beyond the nav/theme now that it's the homepage with real per-post routing - check `CLAUDE-blog.md` for current architecture, and the open blog items below (draft-flip test content, real `home.md` content, static HTML for full SEO, image optimisation).
- [ ] **`JobApplications/broad-media/`: give it a `CLAUDE.md` and a session note** reconstructing the 28 Jul Broad Media work from the files, and record whether those cover letters were actually sent and to which listings (currently unknown). Consider whether `JobApplications/` needs a per-application folder convention - it currently holds two project folders plus a loose `test.md` and a stray screenshot.
- [ ] Consider a games portfolio endpoint, or point the gaming cover letter somewhere that shows Hardle / Bird Bingo / the Pokémon calculator - it names them but the attached CV only links the technical-writing portfolio
- [ ] Apply the `cv-fde` print fix to `cv/` pre-emptively (allow `.job-entry` to split) - it has the original `break-inside: avoid` and will blank out page 1 the same way if its first job entry grows past eight bullets
- [ ] Optional: `cv/` sits at 5 featured cards in a 2-column grid, so the last row has a gap - fill or leave
- [ ] Test admin login and D1 sync on mobile
- [ ] Verify notes load correctly after DB v3 fix in prod
- [ ] Hardle: continue live device testing (tile sizing/centering fixes were verified via Playwright, not yet confirmed on real devices)
- [ ] Hardle: watch for other state-bleed bugs between Hardle/Practice Mode (shared `game` object instance)
- [ ] Hardle: consider writing a dedicated `hardle/CLAUDE.md`
- [ ] **Blog: flip `content/test/` posts to `draft: true`** - the CLS bug they existed to measure is fixed and live, so the fixture has served its purpose. Five posts, currently public but no longer linked from the homepage (as of session 021, `home.md` is minimal - see below). Keep them in the repo, just drafted. Re-run `node build.js` (or let CI do it) afterwards.
- [ ] **Blog: write real `home.md` content** - as of session 021, the homepage is just "Blog coming soon." Three sections (Mobile Application Development, Modding, Agentic Engineering - 6 stub posts total) were deliberately pulled and saved to `DRAFT-TOPICS.md` at the repo root for the user to write about properly later; re-add via the `` ```link:/url `` fenced-block syntax documented in `CLAUDE-blog.md` once ready.
- [ ] Blog: consider static HTML generation in `build.js` for full per-post SEO - posts now have their own URL and `<title>`/meta description (session 016), but content is still rendered client-side from fetched markdown, so a crawler that doesn't run JS sees an empty shell. SEO 100 is mechanical checks passing, not this being fully solved. **Same fix would also give real per-post Open Graph previews** (session 022 added site-wide-only `og:*`/`twitter:*` tags since scrapers don't run JS and can't see the client-side per-post title/description app.js already sets).
- [ ] Blog: consider build-step image optimisation (payload size; the dimension-reading half is already done)
- [ ] Bird Bingo: run `budget.html` once the eBird 429 clears (`git checkout archive/birdle-tests-merlin-research`, serve `birdle-tests/merlin-reverse-engineering/`, open it, "Run sweep") - 3 methods x 5 shrinking API budgets, never completed a clean run. Judge the bird lists to find where quality collapses.
- [ ] Bird Bingo: validate the Merlin finding at n=2 - capture a second Merlin list from a different region/season before building anything on one data point
- [ ] Bird Bingo: decide the ranking architecture (see `METHODS.md`) - likely hybrid: static bar-chart table where captured -> cached computed frequency -> live compute -> current method as last resort, all behind one `{speciesCode, comName, sciName, score}` contract
- [ ] Bird Bingo: fix the fictional `"N checklists"` metric string in `bingo.js` regardless of which ranking source wins
- [ ] Bird Bingo: finish verifying wave 2 (theme/icons) - click the Light toggle and confirm the page re-themes correctly, check daily/practice/life/bird pages' headers and icons render right (only the homepage was re-checked after wave 2 landed, browser session got interrupted)
- [ ] Bird Bingo: live-verify Share Score (native share sheet) and the full celebration flow on a real phone
- [ ] Bird Bingo: check mobile layout at a few more breakpoints (only 390px was checked in session 009; resize tool errored out partway through)
- [ ] Bird Bingo: real-device GPS test - a `kCLErrorLocationUnknown` warning showed up in Chrome desktop after session 009 (likely just a macOS Core Location hiccup, not an app bug, but worth confirming location capture works reliably on an actual phone)
- [ ] Bird Bingo: consider distinct app icons (currently placeholder copies of Birdle's) - now more visible than before since the header's Home button uses this icon directly on every page
- [ ] Birdle: still no `birdle/CLAUDE.md` despite being referenced elsewhere
- [x] ~~Clean up session-008 leftovers: uncommitted `birdle-tests/garden-birds.html`~~ - **done in session 022**: moved to branch `archive/birdle-tests-merlin-research` along with the session-012 Merlin research, off `dev`/`main`'s working tree.
