# Cloudflare Pages / Netlify redirects

# Domain-wide redirect from old domain to new domain
https://wecreatethis.pages.dev/* https://wecreatethis.com/:splat 301

# Client-side routing support for sub-apps

# Every other top-level app folder (session 027: these silently stopped
# resolving once the blog homepage move added the final /* /section 200
# catch-all - the wildcard always wins over a real static file/folder per
# Cloudflare Pages docs, "redirects are always followed regardless of
# whether an asset matches the incoming request" - so every app needs an
# explicit pass-through here or it renders the blog shell instead of itself.
# Rule targets use :splat, not a literal /app/index.html - Cloudflare's
# build-time loop detector silently drops any rule whose destination would
# re-normalize back to match its own source pattern (e.g. /app/index.html
# stripping back down to /app/), and does so with no runtime error, just a
# build-log warning nobody sees. Two lines per app: the bare path (no
# trailing slash) redirects to the trailing-slash form, since a lone
# /app/* splat requires something after the slash to match.
/tarot  /tarot/  301
/tarot/*  /tarot/:splat  200
/birdle  /birdle/  301
/birdle/*  /birdle/:splat  200
/beautiful-mind  /beautiful-mind/  301
/beautiful-mind/*  /beautiful-mind/:splat  200
/bird-bingo  /bird-bingo/  301
/bird-bingo/*  /bird-bingo/:splat  200
/hardle  /hardle/  301
/hardle/*  /hardle/:splat  200
/git-notes  /git-notes/  301
/git-notes/*  /git-notes/:splat  200
/perfectday  /perfectday/  301
/perfectday/*  /perfectday/:splat  200
/starrynight  /starrynight/  301
/starrynight/*  /starrynight/:splat  200
/symbolicritual  /symbolicritual/  301
/symbolicritual/*  /symbolicritual/:splat  200
/voice-notes  /voice-notes/  301
/voice-notes/*  /voice-notes/:splat  200
/chessle  /chessle/  301
/chessle/*  /chessle/:splat  200
/chesstimer  /chesstimer/  301
/chesstimer/*  /chesstimer/:splat  200
/hiresurge  /hiresurge/  301
/hiresurge/*  /hiresurge/:splat  200
/inkwell  /inkwell/  301
/inkwell/*  /inkwell/:splat  200
/longform  /longform/  301
/longform/*  /longform/:splat  200
/pokemon  /pokemon/  301
/pokemon/*  /pokemon/:splat  200
/rondevlei  /rondevlei/  301
/rondevlei/*  /rondevlei/:splat  200
/slowread  /slowread/  301
/slowread/*  /slowread/:splat  200

# Blog is now the homepage: every post is a flat route (/{post-slug}),
# resolved client-side by app.js - no server routing needed per post. Real
# files must pass through first (first match wins), then any other path
# serves the post shell. Adding a content folder needs NO changes here -
# build.js is what rejects a new post whose slug would collide with one of
# these names (see the reserved-name check in checkCollisions).
/  /  200
/content/*  /content/:splat  200
/app.js  /app.js  200
/icons.js  /icons.js  200
/sw-toast.js  /sw-toast.js  200
/style.css  /style.css  200
/sw.js  /sw.js  200
/manifest.json  /manifest.json  200
/content-manifest.json  /content-manifest.json  200
/sitemap.xml  /sitemap.xml  200
/robots.txt  /robots.txt  200
/llms.txt  /llms.txt  200
/icon-192.png  /icon-192.png  200
/icon-512.png  /icon-512.png  200
/favicon.svg  /favicon.svg  200
/profile.jpeg  /profile.jpeg  200
/apple-touch-icon.png  /apple-touch-icon.png  200
/og-image.png  /og-image.png  200
/fonts/*  /fonts/:splat  200
/archive/*  /archive/:splat  200

# Legacy post URLs from before the flat-URL migration (session: flat URLs +
# tag homepage). Each was /{old-section}/{old-slug}; the new flat URL is
# /{slug} (the post's own slug: front matter if it needed one to avoid a
# collision, else unchanged). One line per post that was ever live under
# the old scheme - not generated/hand-maintained automatically, so re-derive
# this block (diff content-manifest.json's `source` vs `slug` per post)
# whenever a post's folder or slug changes after publishing.
/harness-customisation/a-skill-structure-that-improves-itself  /a-skill-structure-that-improves-itself  301
/heat/stub  /heat-stub  301
/kindlewriter/stub  /kindlewriter-stub  301
/lotus/stub  /lotus-stub  301
/agent-hacking/stub  /agent-hacking-stub  301
/agent-programmatics/stub  /agent-programmatics-stub  301
/misc/agent-starts-taking-screenshots-on-auto-mode  /agent-starts-taking-screenshots-on-auto-mode  301
/recipes/gochujang-chicken-ramen  /gochujang-chicken-ramen  301

# Legacy section-index URLs. A section page listed only that folder's posts;
# the nearest equivalent now is the homepage filtered to that folder's name
# as a tag (every post above kept its old group name as one of its tags -
# see the tag retrofit in the same migration), so these land somewhere
# genuinely related rather than just bouncing to a blank home page.
/harness-customisation  /?tag=harness-customisation  301
/heat  /?tag=heat  301
/kindlewriter  /?tag=kindlewriter  301
/lotus  /?tag=lotus  301
/agent-hacking  /?tag=agent-hacking  301
/agent-programmatics  /?tag=agent-programmatics  301
/misc  /?tag=misc  301
/recipes  /?tag=recipes  301

# CV / portfolio pages - static, not part of the blog's routing, but caught
# by the wildcard below without an explicit pass-through (session 027: these
# stopped serving once the blog homepage move added the final catch-all).
# Two rules per app: the bare path (no trailing slash) needs its own redirect
# to the trailing-slash form since a lone /cv/* splat requires something
# after the slash to match - /cv alone falls through without this.
/cv  /cv/  301
/cv/*  /cv/:splat  200
/cv-fde  /cv-fde/  301
/cv-fde/*  /cv-fde/:splat  200
/portfolio-fde  /portfolio-fde/  301
/portfolio-fde/*  /portfolio-fde/:splat  200
/technical-writing-portfolio  /technical-writing-portfolio/  301
/technical-writing-portfolio/*  /technical-writing-portfolio/:splat  200

/*  /post  200
